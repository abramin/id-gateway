CRENE_ENV=demo
DEMO_MODE=true
ID_GATEWAY_ADDR=:8080
REGULATED_MODE=false
# Demo uses built-in signing key; override cautiously for local experiments only.
# DEMO_JWT_ISSUER=credo-demo
CONSENT_TTL=8760h
CONSENT_GRANT_WINDOW=1s
TOKEN_TTL=15m
SESSION_TTL=24h
# Permissive CORS for local UI
CORS_ALLOW_ORIGINS=*
